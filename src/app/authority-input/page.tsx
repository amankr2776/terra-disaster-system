import { AuthorityInputClient } from "./AuthorityInputClient"

export const metadata = {
  title: 'TERRA | Authority Command',
  description: 'Manual disaster scenario input and simulation control for emergency authorities',
}

export default function AuthorityInputPage() {
  return <AuthorityInputClient />
}
