import { ResourcesClient } from "./ResourcesClient"

export const metadata = {
  title: 'TERRA | Resource Management',
  description: 'Emergency resource allocation and deployment management for disaster response teams',
}

export default function ResourcesPage() {
  return <ResourcesClient />
}
